[project]
name = "catena"
version = "0.1.0"
description = "Berean retrieval and citation service — the model layer"
requires-python = ">=3.12"
license = "Apache-2.0"
# Only what the current task needs. The embedder (Task 5) and the acquisition
# pipeline (Task 4) each add their own; a dependency added before its task is a
# dependency nobody can justify at review.
#
# These two are Task 2's: the generated stubs in `gen/` import `google.protobuf`
# and `grpc`, so a contract that cannot be imported is not a contract that has
# been delivered. Exact versions are pinned in uv.lock. The floors below are the
# runtime versions the pinned codegen plugins in buf.gen.yaml emit against —
# protobuf's generated code checks its runtime version at import.
dependencies = [
    "protobuf>=6.33.1,<7",
    "grpcio>=1.76.0,<2",
    # Task 4's one dependency. `corpora/<corpus-id>/manifest.yaml` is a
    # committed, hand-reviewed provenance record, and YAML is what the
    # acquisition contract specifies it in. Fetch is stdlib `urllib` and HTML
    # extraction is stdlib `html.parser`, so the offline overlay stays honest
    # and no acquisition pulls a parser at run time.
    "pyyaml>=6.0.2,<7",
    # The BCO's only source is a 423-page PDF: pcaac.org serves the text
    # through a JavaScript application (230 KB of markup over 0.5% text), and
    # no bare-text edition exists. Extraction was stdlib until here --
    # `html.parser` and `zipfile` -- and there is no stdlib route to a PDF, so
    # this is the one corpus that costs a parser. pypdf is BSD-3-Clause, pure
    # Python, and has no runtime dependencies of its own, so the offline
    # overlay stays honest and `docker compose up` still needs no account.
    "pypdf>=6.0,<7",
    # Task 5's two. Ingestion is the first thing in this project that writes the
    # corpus tables and the first that runs a model, so it is the first that
    # needs a driver or an encoder.
    #
    # psycopg 3 is the Postgres driver. LGPL-3.0, which permits the commercial
    # downstream use CLAUDE.md tells us to assume: it is a library used through
    # its published interface, not linked-and-modified, so it places no term on
    # this project's Apache-2.0 licence. `[binary]` ships the wheels, so the
    # image needs no libpq build step.
    "psycopg[binary]>=3.2,<4",
    # BGE-M3 runs in-process from /models/ rather than through Ollama, whose
    # `bge-m3` exposes dense vectors only and drops the learned sparse
    # representation the model was chosen for (ADR-0006, TECHNICAL-SPEC).
    # sentence-transformers is Apache-2.0 and reads the weights layout the
    # pinned download already has -- modules.json, 1_Pooling,
    # sentence_bert_config.json -- so `max_seq_length` comes from the model
    # rather than from a constant this repository would have to keep true.
    "sentence-transformers>=3.0,<6",
    # Declared even though sentence-transformers already pulls it. `tool.uv.sources`
    # below applies to a project's *direct* dependencies only, and torch arriving
    # transitively is what let the CUDA build in -- so naming it here is what makes
    # the CPU pin take effect at all, not a restatement of something implied.
    "torch>=2.0",
    # Task 7's one dependency, and the first thing in this project to call a
    # model in the request path. SHARED §6 requires Langfuse instrumentation
    # "from Phase 1" and ADR-0009 chose it over LangSmith; retrofitting means
    # the Phase 1 baseline is unmeasured, which defeats measuring Phase 3
    # against it.
    #
    # MIT, and it brings OpenTelemetry with it -- which SHARED §6 also asks for
    # on the non-LLM path, so the transitive weight is a dependency this project
    # owes anyway rather than one Langfuse imposes. Roughly 30 MB against the
    # ~2.3 GB of encoder already in this image.
    #
    # The local generator is stdlib `urllib` (see catena/serve/generate/ollama.py),
    # on the same reasoning that keeps `acquire.fetch` on stdlib. The hosted
    # provider is not; see the `anthropic` entry below for why that is a
    # reversal rather than an inconsistency.
    "langfuse>=4.15,<5",
    # The standard gRPC health service, so compose can tell "the port is open"
    # from "the encoder is loaded". Catena joins the default `up` in Task 7 and
    # the gateway depends on it, and BGE-M3 takes tens of seconds to load from
    # /models -- a TCP check would report healthy through the whole of it. It is
    # the generated health stubs and a servicer, nothing more.
    "grpcio-health-checking>=1.76,<2",
    # The hosted generation provider (BYOK-GENERATION-DESIGN.md). MIT.
    #
    # This is the first vendor SDK in the request path, and it is a considered
    # reversal: the comment on langfuse above says the OpenAI-compatible *wire
    # format* is what makes providers interchangeable, "not a vendor SDK". That
    # was written when there was one provider and so had no way to be tested.
    # What actually delivers interchangeability is the `Generator` protocol --
    # `service.py` depends on that and on nothing else -- and the Claude API is
    # not OpenAI-shaped, so a second provider is exactly the case that
    # distinguishes the two claims.
    #
    # It is not in the default path: `CATENA_GENERATION_PROVIDER` defaults to
    # `ollama`, the module is imported only when a deployer selects it, and
    # `make dev-offline` fails loudly if it is ever reached by accident.
    "anthropic>=0.75",
]

[project.scripts]
catena = "catena.__main__:main"

# torch, pulled in by sentence-transformers, resolves to the CUDA build on Linux
# by default -- several GB of nvidia-* wheels that cannot execute on the machine
# the acceptance test targets. SHARED §1 is `docker compose up` on a 16 GB host
# with Ollama holding Qwen3-8B resident, and ADR-0006 chose BGE-M3 for being
# CPU-tolerable, so the CUDA libraries are dead weight in the one image that has
# to fit. This pins the CPU wheel for the platforms that have a CUDA default.
#
# macOS is deliberately not in the marker: PyPI's macOS wheels are already
# CPU-only, and download.pytorch.org does not carry an arm64 build for every
# release, so routing darwin through the CPU index would break the dev host to
# fix the container. Nothing about the model layer differs between the two --
# the weights, the tokeniser and `max_seq_length` are the same either way.
[tool.uv.sources]
torch = [
    { index = "pytorch-cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# `berean` is the generated contract, committed rather than generated at build
# time (ADR-0022). It is packaged here rather than reached through PYTHONPATH so
# that an editable dev install and the wheel in the image resolve it the same
# way -- an import that works in one and not the other is the kind of difference
# that surfaces two tasks later as something else.
[tool.hatch.build.targets.wheel]
packages = ["src/catena", "gen/berean"]

[tool.ruff]
line-length = 100
